<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <%= form_for(@topic) do |f| %>
      <% if @topic.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this song from being saved:</h2>

          <ul>
          <% @topic.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.select :subjectx, ["Math","English","Science"], {:include_blank => 'Subject'} %>
      </div>
      <div class="field">
        <%= f.label :student_id %><br>
        <%= f.text_field :student_id, value: @current_teacher.id %>
      </div>
      
      <div class="actions">
    <%= f.submit %>
  </div>
    <% end %>
  </div>