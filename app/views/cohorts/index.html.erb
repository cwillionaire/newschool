<p id="notice"><%= notice %></p>

<h1>Whatever, this is just a header</h1>

<table>
  <thead>
    <tr>
      <th>Subject</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @cohort.each do |x| %>
      <tr>
        <td><%= x.subject %></td>
        <td><%= link_to 'Show', x %></td>
        <td><%= link_to 'Edit', edit_cohort_path(x) %></td>
        <td><%= link_to 'Destroy', x, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cohort', new_cohort_path %>